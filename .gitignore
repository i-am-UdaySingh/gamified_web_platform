# --------------------------------------------------------------------------
# 1. DEPENDENCY DIRECTORIES
# These folders contain thousands of files downloaded by package managers.
# They are massive and can always be recreated using package.json, requirements.txt, etc.
# --------------------------------------------------------------------------
node_modules/
venv/
.venv/
vendor/

# --------------------------------------------------------------------------
# 2. BUILD/COMPILED OUTPUT DIRECTORIES
# Files generated when you compile your code (e.g., JavaScript bundles,
# compiled Python files, or Java JARs).
# --------------------------------------------------------------------------
/dist
/build
/out
/target
# For Webpack/Vite/Create-React-App:
/public/dist

# --------------------------------------------------------------------------
# 3. LOGS, CACHE, and TEMP FILES
# These are temporary files generated by your application or environment.
# --------------------------------------------------------------------------
*.log
npm-debug.log*
yarn-error.log*
tmp/
.temp/
# Python bytecode (if using Python for backend)
__pycache__/
*.pyc
*.pyd

# --------------------------------------------------------------------------
# 4. ENVIRONMENT and SENSITIVE FILES
# NEVER commit files containing secrets, API keys, or database credentials.
# --------------------------------------------------------------------------
.env
.env.*
config.local.js
secrets.json

# --------------------------------------------------------------------------
# 5. OS-SPECIFIC FILES
# Generated by your operating system (Mac/Windows) for caching/indexing.
# --------------------------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini

# --------------------------------------------------------------------------
# 6. IDE / EDITOR SPECIFIC FILES (Add only for the IDE you use)
# Customize these for your specific editor (e.g., VS Code, Sublime, JetBrains)
# --------------------------------------------------------------------------
# VS Code
.vscode/

# JetBrains (IntelliJ, PyCharm, WebStorm, etc.)
.idea/
*.iml
*.iws
*.ipr

# Sublime Text
*.sublime-workspace